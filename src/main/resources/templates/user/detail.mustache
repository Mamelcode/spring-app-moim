<html>
<head>
<link rel="stylesheet" type="text/css"
	href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
<link rel="stylesheet" type="text/css" href="profileone.css">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="/css/initial.css"/>
<link rel="stylesheet" href="/css/style.css?"/>
<link
	href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css"
	rel="stylesheet">
<script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
<style type="text/css">
#profile-image1:hover {
	cursor: pointer;
}

.row {
	margin-top: 30px;
}

.profile-badge {
	border: 1px solid #000;
	border-radius: 5px;
}

.btn-info {
	margin-bottom: 10px;
}

.btn_box {
	display: flex;
	justify-content: space-between;
	align-items: center;
}
</style>
</head>
<body>
<div class="container">
	{{#error}}
		<p style="color: red;">모든 정보를 입력해주세요.</p>
	{{/error}}
	<div class="row">
		<div class="col-md-4 col-md-offset-4 col-sm-6 col-xs-12 profile-badge">
			<form class="user-detail text-center" method="post" action="/user/detail">
			<div class="profile-pic" style="text-align: center; margin-top: 20px;">
				
				{{#url}}
				<img src="{{url}}" id="profile-image1" height="200">
				{{/url}}
				{{^url}}
				<img src="https://d30y9cdsu7xlg0.cloudfront.net/png/138926-200.png"
					id="profile-image1" height="200">
				{{/url}}
					
				<!--  
				<input id="profile-image-upload" class="hidden" type="file"
					onchange="previewFile()">
				-->
				<div style="color: #999;"></div>

			</div>
				<div class="form-group">
					<label for="Fname">자기소개</label>
					
					{{#detail}}
					<input type="text" class="form-control" id="Fname" placeholder="자기소개.."
						name="description" value="{{#description}}{{description}}{{/description}}">
					{{/detail}}
					{{^detail}}
					<input type="text" class="form-control" id="Fname" placeholder="자기소개.."
						name="description">
					{{/detail}}
					
				</div>

				<div class="form-group">
					<label for="Lname">주소</label>
					{{#detail}}
					<input type="text" class="form-control" id="Lname" placeholder="주소.."
						name="address" value="{{#address}}{{address}}{{/address}}">
					{{/detail}}
					{{^detail}}
					<input type="text" class="form-control" id="Lname" placeholder="주소.."
						name="address">
					{{/detail}}
				</div>

				<div class="form-group">
					<label for="mobile">생일</label>
					<input type="date" class="form-control" id="Mobile" name="birthday">
				</div>
				
				<div class="people_box">
					<h4><span>*</span> 사용할 아바타</h4>
					<div class="people_wrap">
						{{#avatars}}
						<div class="people_img" style="margin-bottom: 5px;">
							<label for="{{id}}">
								<img src="{{url}}"/>
							</label>
							<input type="radio" value="{{id}}" name="avatarId"
									id="{{id}}"/>
						</div>
						{{/avatars}}
					</div>
				</div>
				<div class="btn_box">
					<button type="submit" class="btn btn-info" style="border: none;">정보수정</button>
					<a href="/user/delete" class="btn btn-info" style="background: #FF82FF; border: none;"
					id="delete_btn">회원탈퇴</a>
				</div>
			</form>
		</div>
	</div>
	<p>개설한 모임 목록</p>
	<ul>
	{{#moimList}}
		<li><a href="/moim/view?moimId={{id}}">{{title}}</a></li>
	{{/moimList}}
	</ul>
</div>
<!--  
<script type="text/javascript">
	function previewFile() {
		var preview = document.querySelector('img');
		var file = document.querySelector('input[type=file]').files[0];
		var reader = new FileReader();

		reader.addEventListener("load", function() {
			preview.src = reader.result;
		}, false);

		if (file) {
			reader.readAsDataURL(file);
		}
	}
	$(function() {
		$('#profile-image1').on('click', function() {
			$('#profile-image-upload').click();
		});
	});
</script>
-->
</body>
</html>
